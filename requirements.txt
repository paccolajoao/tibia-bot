# Runtime — Bot Tibia (Windows). Python 3.12 ou 3.13.

# Captura de tela (a fábrica escolhe: DXGI -> WGC -> mss)
bettercam==1.0.0 ; python_version < "3.13"   # DXGI rápido; pulado no 3.13
windows-capture>=2.0.0 ; sys_platform == "win32"  # Windows Graphics Capture: pega DX12/OpenGL, roda no 3.13
mss>=9.0.1                                     # fallback Python puro/GDI (não captura DX12)

# Visão computacional
opencv-python>=4.10.0
numpy>=2.1.0

# Simulação de input (SendInput; funciona em jogos DirectX onde pyautogui falha)
pydirectinput-rgx>=2.1.0

# Dashboard web (uvicorn[standard] já traz websockets/httptools)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Config
PyYAML>=6.0.2
pydantic>=2.9.0

# Windows: hotkeys globais + foco de janela
keyboard>=0.13.5
pygetwindow>=0.0.9
pywin32>=308
